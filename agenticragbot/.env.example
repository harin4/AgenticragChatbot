# Production DB (or default local dev)
DATABASE_URL="postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# Jina API key for scraping (Layer 1 crawl — Phase 2)
JINA_API_KEY="jina_..."

# Bearer token for Cloudflare Worker API (optional locally if unset)
API_KEY="your-secret-token"
