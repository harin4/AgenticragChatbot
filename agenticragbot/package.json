{
  "name": "kb-formation",
  "version": "1.1.0",
  "description": "Agentic RAG — Layer 1: KB Formation Worker. Crawls websites, converts to markdown, stores in Neon DB.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env dev",
    "tail": "wrangler tail",
    "db:init": "node init-db.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.3"
  },
  "devDependencies": {
    "wrangler": "^3.40.0"
  }
}